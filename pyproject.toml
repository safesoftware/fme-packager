[build-system]
requires = [
    "setuptools",
]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 100
target-version = ['py37']

[tool.coverage.run]
source = [
    "fme_packager",
    "tests",
]
omit = [
    "*/tests/fixtures/**",
    "*/venv/**",
]
