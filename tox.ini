[tox]
min_version = 4.16.0
env_list = py{39,310,311,312,313}

[testenv]
description = run tests
passenv =
    FME_HOME
deps = .[dev]
commands =
    pytest -m ""

[testenv:format]
commands =
    ruff format --check

[testenv:check]
commands =
    ruff check
